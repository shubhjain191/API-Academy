import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "API Academy - Learn API Development",
  description:
    "Comprehensive learning platform for API development concepts, best practices, and implementation guides.",
  keywords: "API, REST, GraphQL, HTTP, Web Development, Programming, Tutorial",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
